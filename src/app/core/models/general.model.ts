export enum GenderEnum {
  Female = 'Female',
  Male = 'Male',
}

export enum CurrencyEnum {
  GEL = 'GEL',
  USD = 'USD',
  EUR = 'EUR',
}

export interface CommonOptionType {
  id: number;
  name: string;
}
