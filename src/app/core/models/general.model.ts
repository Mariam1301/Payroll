export enum GenderEnum {
  Female = 'Female',
  Male = 'Male',
}

export enum PaymentTypeEnum {
  Net = "net",
  Gross= "gross"
}

export enum CurrencyEnum {
  GEL = 'GEL',
  USD = 'USD',
  EUR = 'EUR',
}
